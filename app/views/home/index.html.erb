<!-- Hero Section -->
<div class="bg-white">
 

  <!-- Main Content Wrapper -->
  
    <!-- Hero Section -->
    <div class="bg-gradient-to-br from-blue-50 to-indigo-100">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-20">
        <div class="text-center">
          <h1 class="text-4xl md:text-6xl font-bold text-gray-900 mb-6">
            Accomplish your
            <span class="text-blue-600">to-do list</span>
          </h1>
          <p class="text-xl text-gray-600 mb-12 max-w-2xl mx-auto">
            From home repairs to personal training, find trusted local professionals for any project.
          </p>

          
          <!-- Functional Search Bar -->
          <div class="max-w-2xl mx-auto mb-12">
            <div class="bg-white rounded-lg shadow-lg p-2">
              <%= form_with url: search_services_path, method: :get, local: true, class: "flex flex-col md:flex-row gap-2" do |f| %>
                <div class="flex-1">
                  <%= f.text_field :q, 
                      value: params[:q],
                      placeholder: "What service do you need?",
                      class: "w-full px-4 py-3 text-lg border-0 focus:ring-0 focus:outline-none rounded-md" %>
                </div>
                <div class="flex-1">
                  <%= f.text_field :location, 
                      value: params[:location],
                      placeholder: "Enter your location", 
                      class: "w-full px-4 py-3 text-lg border-0 focus:ring-0 focus:outline-none rounded-md border-l border-gray-200" %>
                </div>
                <%= f.submit "Search", 
                    class: "bg-blue-600 hover:bg-blue-700 text-white px-8 py-3 rounded-md font-semibold text-lg transition-colors cursor-pointer" %>
              <% end %>
            </div>
          </div>
          <!-- Quick Action Buttons -->
          <div>
          <% if user_signed_in? && current_user.is_a?(Client) %>
          <%= link_to "View Your Bookings", client_dashboard_path, class: "bg-white text-blue-600 px-8 py-3 rounded-md font-semibold text-lg border-2 border-blue-600 hover:bg-blue-600 hover:text-white transition-colors" %>
          <% elsif user_signed_in? && current_user.is_a?(ServiceProvider)%>
          <%= link_to "View Dashboard", provider_dashboard_path, class: "bg-white text-blue-600 px-8 py-3 rounded-md font-semibold text-lg border-2 border-blue-600 hover:bg-blue-600 hover:text-white transition-colors" %>
          <% else %>
          <div class="flex flex-col sm:flex-row gap-4 justify-center items-center">
            <%= link_to new_user_registration_path(type: "Client"), 
                class: "bg-white text-blue-600 px-8 py-3 rounded-md font-semibold text-lg border-2 border-blue-600 hover:bg-blue-50 transition-colors",
                data: { turbo_frame: "signup_section" } do %>
              I Need Services
            <% end %>
            <%= link_to new_user_registration_path(type: "ServiceProvider"), 
                class: "bg-blue-600 text-white px-8 py-3 rounded-md font-semibold text-lg hover:bg-blue-700 transition-colors",
                data: { turbo_frame: "signup_section" } do %>
              I'm a Professional
            <% end %>
          </div>
          <% end %>
          
        </div>
      </div>
    </div>

    <%= turbo_frame_tag "signup_section" do %>

      
    <% if current_user.nil? || current_user.type != "ServiceProvider"%>
    <!-- Popular Services -->
    <div class="bg-white py-16">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <h2 class="text-3xl font-bold text-center text-gray-900 mb-12">Popular Services</h2>
        
        <div class="grid grid-cols-2 md:grid-cols-4 lg:grid-cols-6 gap-6">
          <!-- Electrician -->
          <%= link_to category_subcategories_path('electrician'), data: { turbo_frame: "_top" } do %>
            <div class="text-center group cursor-pointer">
              <div class="bg-gray-100 rounded-full w-16 h-16 mx-auto flex items-center justify-center group-hover:bg-gray-200 transition-colors mb-3">
                <!-- Electrician SVG -->
                <svg class="w-8 h-8 text-yellow-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"></path>
                </svg>
              </div>
              <p class="font-medium text-gray-900">Electrician</p>
            </div>
          <% end %>

          <!-- Plumber -->
          <%= link_to category_subcategories_path('plumber'), data: { turbo_frame: "_top" } do %>
          <div class="text-center group cursor-pointer">
            <div class="bg-gray-100 rounded-full w-16 h-16 mx-auto flex items-center justify-center group-hover:bg-gray-200 transition-colors mb-3">
              <!-- Plumber SVG -->
              <svg class="w-8 h-8 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16"></path>
              </svg>
            </div>
            <p class="font-medium text-gray-900">Plumber</p>
          </div>
          <% end %>

          <!-- Home Cleaning -->
          <%= link_to category_subcategories_path('house-cleaner'), data: { turbo_frame: "_top" } do %>
          <div class="text-center group cursor-pointer">
            <div class="bg-gray-100 rounded-full w-16 h-16 mx-auto flex items-center justify-center group-hover:bg-gray-200 transition-colors mb-3">
              <!-- Cleaning SVG -->
              <svg class="w-8 h-8 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 7v10a2 2 0 002 2h14a2 2 0 002-2V9a2 2 0 00-2-2H5a2 2 0 00-2-2v0"></path>
              </svg>
            </div>
            <p class="font-medium text-gray-900">Home Cleaning</p>
          </div>
          <% end %>

          <!-- Carpenter -->
          <%= link_to category_subcategories_path('carpenter'), data: { turbo_frame: "_top" } do %>
          <div class="text-center group cursor-pointer">
            <div class="bg-gray-100 rounded-full w-16 h-16 mx-auto flex items-center justify-center group-hover:bg-gray-200 transition-colors mb-3">
              <!-- Carpenter SVG -->
              <svg class="w-8 h-8 text-amber-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19.428 15.428a2 2 0 00-1.022-.547l-2.387-.477a6 6 0 00-3.86.517l-.318.158a6 6 0 01-3.86.517L6.05 15.21a2 2 0 00-1.806.547M8 4h8l-1 1v5.172a2 2 0 00.586 1.414l5 5c1.26 1.26.367 3.414-1.415 3.414H4.828c-1.782 0-2.674-2.154-1.414-3.414l5-5A2 2 0 009 10.172V5L8 4z"></path>
              </svg>
            </div>
            <p class="font-medium text-gray-900">Carpenter</p>
          </div>
          <% end %>

          <!-- Gardener -->
          <%= link_to category_subcategories_path('gardener'), data: { turbo_frame: "_top" } do %>
          <div class="text-center group cursor-pointer">
            <div class="bg-gray-100 rounded-full w-16 h-16 mx-auto flex items-center justify-center group-hover:bg-gray-200 transition-colors mb-3">
              <!-- Gardener SVG -->
              <svg class="w-8 h-8 text-emerald-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 3v4M3 5h4M6 17v4m-2-2h4m5-16l2.286 6.857L21 12l-5.714 2.143L13 21l-2.286-6.857L5 12l5.714-2.143L13 3z"></path>
              </svg>
            </div>
            <p class="font-medium text-gray-900">Gardener</p>
          </div>
          <% end %>

          <!-- Mechanic -->
          <%= link_to category_subcategories_path('mechanic'), data: { turbo_frame: "_top" } do %>
          <div class="text-center group cursor-pointer">
            <div class="bg-gray-100 rounded-full w-16 h-16 mx-auto flex items-center justify-center group-hover:bg-gray-200 transition-colors mb-3">
              <!-- Mechanic Gear SVG -->
              <svg class="w-8 h-8 text-red-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z"></path>
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"></path>
              </svg>
            </div>
            <p class="font-medium text-gray-900">Mechanic</p>
          </div>
          <% end %>
        </div>
    </div>
        <div class="text-center mt-8">
          <%= link_to "See all services →", categories_path, data: {turbo_frame: "_top"}, class:"text-blue-600 font-semibold hover:text-blue-700" %>
        </div>
      </div>
    </div>
    <% end %>

    <!-- How It Works -->
    <div class="bg-gray-50 py-16">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <h2 class="text-3xl font-bold text-center text-gray-900 mb-12">How Helpster Works</h2>
        
        <div class="grid md:grid-cols-3 gap-8">
          <div class="text-center">
            <div class="bg-blue-100 rounded-full w-16 h-16 flex items-center justify-center mx-auto mb-4">
              <span class="text-2xl font-bold text-blue-600">1</span>
            </div>
            <h3 class="text-xl font-semibold text-gray-900 mb-2">Tell us what you need</h3>
            <p class="text-gray-600">Answer a few questions about your project so we can match you with the right pros.</p>
          </div>

          <div class="text-center">
            <div class="bg-blue-100 rounded-full w-16 h-16 flex items-center justify-center mx-auto mb-4">
              <span class="text-2xl font-bold text-blue-600">2</span>
            </div>
            <h3 class="text-xl font-semibold text-gray-900 mb-2">Get matched with pros</h3>
            <p class="text-gray-600">We'll introduce you to verified professionals in your area who can help.</p>
          </div>

          <div class="text-center">
            <div class="bg-blue-100 rounded-full w-16 h-16 flex items-center justify-center mx-auto mb-4">
              <span class="text-2xl font-bold text-blue-600">3</span>
            </div>
            <h3 class="text-xl font-semibold text-gray-900 mb-2">Book with confidence</h3>
            <p class="text-gray-600">Review quotes, read reviews, and book the pro that's right for your project.</p>
          </div>
        </div>
      </div>
    </div>

    <!-- For Professionals -->
    <div class="bg-blue-600 py-16">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
        <h2 class="text-3xl font-bold text-white mb-4">Ready to grow your business?</h2>
        <p class="text-xl text-blue-100 mb-8 max-w-2xl mx-auto">
          Join thousands of professionals who use Helpster to connect with new customers and grow their business.
        </p>
        <% if current_user.nil? || current_user.type != "ServiceProvider" %>
          <%= link_to new_user_registration_path(type: "ServiceProvider"), 
              class: "bg-white text-blue-600 px-8 py-3 rounded-md font-semibold text-lg hover:bg-gray-100 transition-colors inline-block",
              data: { turbo_frame: "signup_section" } do %>
            Get Started as a Pro
          <% end %>
        <% else %>
          <%= link_to provider_dashboard_path, 
              class: "bg-white text-blue-600 px-8 py-3 rounded-md font-semibold text-lg hover:bg-gray-100 transition-colors inline-block",
              data: { turbo_frame: "_top" } do %>
            Go To Dashboard
          <% end %>
        <% end %>

      </div>
    </div>

    <!-- Trust Section -->
    <div class="bg-white py-16">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="grid md:grid-cols-3 gap-8 text-center">
          <div>
            <div class="text-4xl font-bold text-blue-600 mb-2">500K+</div>
            <p class="text-gray-600">Completed Projects</p>
          </div>
          <div>
            <div class="text-4xl font-bold text-blue-600 mb-2">50K+</div>
            <p class="text-gray-600">Verified Professionals</p>
          </div>
          <div>
            <div class="text-4xl font-bold text-blue-600 mb-2">4.8★</div>
            <p class="text-gray-600">Average Rating</p>
          </div>
        </div>
      </div>
    </div>
<% end %>
  
</div>