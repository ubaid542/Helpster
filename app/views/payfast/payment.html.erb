<div class="max-w-2xl mx-auto p-6 bg-white rounded-lg shadow-md">
  <div class="text-center mb-6">
    <h2 class="text-2xl font-bold text-gray-800">Complete Your Payment</h2>
    <p class="text-gray-600 mt-2">You will be redirected to PayFast Pakistan to complete your payment</p>
  </div>
  
  <div class="mb-6 p-4 bg-blue-50 rounded-lg border border-blue-200">
    <h3 class="font-semibold text-blue-800 mb-3">Booking Details</h3>
    <div class="space-y-2 text-sm">
      <p><strong>Service:</strong> <%= @booking.service.name %></p>
      <p><strong>Provider:</strong> <%= @booking.service_provider.full_name %></p>
      <p><strong>Date:</strong> <%= @booking.date.strftime('%B %d, %Y') %></p>
      <p><strong>Time:</strong> <%= @booking.time %></p>
      <p><strong>Address:</strong> <%= @booking.address %></p>
    </div>
  </div>

  <div class="mb-6 p-4 bg-green-50 rounded-lg border border-green-200">
    <h3 class="font-semibold text-green-800 mb-3">Payment Information</h3>
    <div class="flex justify-between items-center">
      <span class="text-lg">Total Amount:</span>
      <span class="text-2xl font-bold text-green-600">Rs. <%= number_with_delimiter(@booking.price.to_i) %></span>
    </div>
  </div>

  <div class="mb-6 p-4 bg-yellow-50 rounded-lg border border-yellow-200">
    <h3 class="font-semibold text-yellow-800 mb-3">PayFast Pakistan Test Details</h3>
    <p class="text-sm text-yellow-700 mb-2">Use these details for testing in PayFast Pakistan sandbox:</p>
    <div class="text-sm space-y-1">
      <p><strong>Bank Name:</strong> Demo Bank</p>
      <p><strong>Account No:</strong> 12353940226802034243</p>
      <p><strong>CNIC Number:</strong> 4210131315089</p>
      <p><strong>OTP:</strong> 123456</p>
    </div>
  </div>

  <%= form_tag payfast_process_payment_path, method: :post, data: { turbo: false } do %>
        <%= hidden_field_tag :booking_id, @booking.id %>
    
    <div class="flex space-x-4">
      <%= submit_tag "Proceed to PayFast Pakistan", 
          class: "flex-1 bg-green-600 hover:bg-green-700 text-white font-bold py-3 px-6 rounded-lg transition duration-200" %>
      <%= link_to "Cancel", client_dashboard_path, 
          class: "flex-1 bg-gray-500 hover:bg-gray-600 text-white font-bold py-3 px-6 rounded-lg text-center transition duration-200" %>
    </div>
  <% end %>

  <div class="mt-6 text-center">
    <p class="text-xs text-gray-500">
      🔒 Your payment is secured by PayFast Pakistan - Pakistan's trusted payment gateway
    </p>
    <p class="text-xs text-gray-400 mt-1">
      Powered by <a href="https://gopayfast.com" target="_blank" class="text-blue-500">gopayfast.com</a>
    </p>
  </div>
</div>